/* ================================= JS Code ======================================
CAPINCI 1.00
Business Area: pmo Transaction: ORGANZ
App: ORGANZ_ADM (Organizations)
=================================================================================== */


const cds = require("@sap/cds");

module.exports = async (srv) =>
{
	const { Orgs } = srv.entities;

	/* ----------------------------- HANDLERS -------------------------------------- */

}